[count]

%{~ for instance1 in count ~}

${instance1["name"]}   ansible_host=${instance1["network_interface"][0]["nat_ip_address"]} 
%{~ endfor ~}

[disk_vm]

%{~ for instance2 in disk_vm ~}

${instance2["name"]}   ansible_host=${instance2["network_interface"][0]["nat_ip_address"]} 
%{~ endfor ~}

[forearch]

%{~ for instance2 in forearch ~}

${instance2["name"]}   ansible_host=${instance2["network_interface"][0]["nat_ip_address"]} 
%{~ endfor ~}